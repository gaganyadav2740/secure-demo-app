# Application name
spring.application.name=secure-demo-app

# Server port
server.port=8080

# Database URL
spring.datasource.url=jdbc:mysql://localhost:3306/secure_demo

# Hardcoded username (intentional issue)
spring.datasource.username=admin

# Hardcoded password (intentional issue)
spring.datasource.password=admin123

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
